---
import ContentCard from './ContentCard.astro'
import { Card as ShadcnCard } from './ui/_card'
import { SquareArrowOutUpRight } from 'lucide-react'

interface Props {
  title?: string
  body?: string
  colSpan?: string
  rowSpan?: string
  colStart?: string
  rowStart?: string
  extraClass?: string
  href?: string
  colorText?: string
}

const { title, body, colSpan, rowSpan, colStart, rowStart, extraClass, href, colorText } = Astro.props
---

<ShadcnCard
  className={`card-animate transfor-y-[-40%] align-start perspective-1200 group group relative col-span-1 h-auto w-full self-start flex-none transform justify-start overflow-hidden p-6 shadow-lg transition duration-75 ease-in-out ${
    colSpan || 'md:col-span-2'
  } ${rowSpan || ''} ${colStart || ''} ${rowStart || ''} ${extraClass || ''} ${href ? 'cursor-pointer hover:border-primary' : ''}`}
>
  {
    href ? (
      <a href={href} class={`block h-full w-full ${colorText || ' '}`}>
        <SquareArrowOutUpRight
          className="group-hover:text-primary position absolute right-6 "
          size={20}
        />
        <ContentCard title={title} body={body}>
          <slot />
        </ContentCard>
      </a>
    ) : (
      <ContentCard title={title} body={body}>
        <slot />
      </ContentCard>
    )
  }
</ShadcnCard>
